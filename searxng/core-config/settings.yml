use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  public_instance: false
  secret_key: "replace-this-with-SEARXNG_SECRET-in-searxng-dot-env"

search:
  safe_search: 1
  formats:
    - html
    - json

valkey:
  url: redis://valkey:6379/0
